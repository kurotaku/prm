<!DOCTYPE html>
<html>
  <head>
    <%= render 'layouts/html_header' %>
  </head>

  <body class="center-layout <%= yield(:body_class) %>" id="<%= yield(:body_id) %>">
    <section class="main-col w-100">
      <%= render 'layouts/flash_message' %>
      <%= yield %>
    </section>
  </body>
</html>
