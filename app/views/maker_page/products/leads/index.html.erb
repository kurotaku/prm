<% content_for(:title, 'メーカー - 案件一覧') %>

<header class="primary-header d-flex align-items-center">
  <%= render 'maker_page/products/show_parts/header' %>
</header><!-- .primary-header -->

<div class="secondary-header">
  <%= render 'maker_page/products/show_parts/page_menu' %>
</div><!-- .secondary-header -->

<main class="main-contents">
  
  <div class="d-flex mb-5">
    <%= link_to '案件' + t('common.new'), new_admin_page_user_path, class: 'btn btn-m default before-icon plus ml-auto' %>
  </div>

  <div class="table-wrap">
    <table class="border-table">
      <thead>
        <tr>
          <% @product.lead_index_columns.order(order: 'ASC').each do |index_column| %>
            <td><%= index_column.name %></td>
          <% end %>
        </tr>
      </thead>
      <tbody>
        <% @leads.each do |lead| %>
          <tr>
            <% lead.index_cache.each do |key, value| %>
              <td><%= value %></td>
            <% end %>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div><!-- .table-wrap -->

</main><!-- .main-contents -->